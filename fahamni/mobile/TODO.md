# Flutter Project Fix Errors TODO

## Status: In Progress

### Steps from Approved Plan:

1. **✅** Plan confirmed by user.
2. **✅** Fix main.dart syntax errors (remove stray code, duplicate AppCheck).
3. **✅** Fix lib/Services/auth_.service.dart syntax/logic (missing braces, login(), constructors, etc.).
4. **✅** Update pubspec.yaml with proper dependency versions.
5. **✅** Run `flutter pub get && flutter analyze` to verify.
6. **⏳** Fix google_fonts issues in messaging files (read and edit).
7. **⏳** Fix lib/StudentHomePage/Student_homepage.dart (TODOs, errorBuilder).
8. **⏳** Fix lib/explorepage.dart & lib/widgets/servicedetails.dart (imports/navbar).
9. **⏳** Rename PascalCase files to snake_case.
10. **⏳** Fix async BuildContext issues across project.
11. **⏳** Remove debugPrints.
12. **✅** Upgrade deps & flutter analyze clean (<20 issues).
13. **✅** Test run `flutter run`.

**Next: Execute fixes step-by-step, updating status after each.**
